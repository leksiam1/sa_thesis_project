events {
    worker_connections 1024;
}

http {
    upstream backend {
        server {{ hostvars['vm-03']['ansible_host'] }}:80;
        server {{ hostvars['vm-04']['ansible_host'] }}:80;
    }

    server {
        listen 3000;
        location / {
            proxy_pass http://backend;
        }
    }
}
